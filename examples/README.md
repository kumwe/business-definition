# Example

Run `php examples/definition.php` after composer install. An optional first argument points to the consuming project
autoloader. The example constructs and validates a representative definition, checks deterministic bytes and
compatibility, and exercises actual Laminas DI with an explicit fixture admission policy.
